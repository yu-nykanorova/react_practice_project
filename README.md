# React + TypeScript + Vite

## Homework 1, task #kLZQR7Q98
Take the simpsons array from arrays.js.
The root component is App.
Create a CharacterComponent that will contain all the information about every array object. The layout of the component is sufficient. Give a description of the character through children.
Create a FamilyComponent component that will iterate through the Simpsons array.
The styling is sufficient, but smoothly connected and minimally detailed (at least one style that works).

## Technologies
- React
- Typescript
- Vite
- TailwindCSS

## Getting started
1) Clone the repository
```bash
git clone <repository-url>
```
2) Go to the project folder
```bash
cd <project-folder>
```
3) Install dependencies
```bash
npm install
```
4) Run project
```bash
npm run dev
```
The project will be available at: http://localhost:5173