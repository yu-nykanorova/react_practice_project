# React + TypeScript + Vite

## Homework 4, task #mtNy6QQ

There are following pages:

UsersPage - displays users from the dummyjson.com api (arbitrary markup and amount of object info).
Add “forward” and “back” buttons for pagination of objects.

## Technologies
- React + Vite
- React Router DOM
- Typescript
- Fetch API
- TailwindCSS

## Getting started
1) Clone the repository
```bash
git clone <repository-url>
```
2) Go to the project folder
```bash
cd <project-folder>
```
3) Install dependencies
```bash
npm install
```
4) Run project
```bash
npm run dev
```
The project will be available at: http://localhost:5173